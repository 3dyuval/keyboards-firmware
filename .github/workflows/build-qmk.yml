on:
  push:
    paths:
      - 'config/iris-lm-r1.json'
      - '.github/workflows/build-qmk.yml'
  pull_request:
    paths:
      - 'config/iris-lm-r1.json'
      - '.github/workflows/build-qmk.yml'
  workflow_dispatch:

jobs:
  build:
    uses: qmk/.github/.github/workflows/qmk_userspace_build.yml@main
