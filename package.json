{
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "cli": "bun run --filter keyb dev",
    "flash-totem": "bun --cwd apps/cli1 index.tsx flash totem left -y && bun --cwd apps/cli1 index.tsx flash totem right -y",
    "flash-eyelash": "bun --cwd apps/cli1 index.tsx flash eyelash left -y && bun --cwd apps/cli1 index.tsx flash eyelash right -y",
    "flash-corne": "bun --cwd apps/cli1 index.tsx flash corne left -y && bun --cwd apps/cli1 index.tsx flash corne right -y",
    "flash-iris": "bun --cwd apps/cli1 index.tsx flash iris -y",
    "draw-iris": "NODE_ENV=development bun --cwd apps/cli1 index.tsx draw iris",
    "draw-corne": "NODE_ENV=development bun --cwd apps/cli1 index.tsx draw corne",
    "draw-eyelash": "NODE_ENV=development bun --cwd apps/cli1 index.tsx draw eyelash",
    "draw-totem": "NODE_ENV=development bun --cwd apps/cli1 index.tsx draw totem",
    "draw-all": "bun draw-iris && bun draw-corne && bun draw-eyelash && bun draw-totem",
    "draw": "NODE_ENV=development bun --cwd apps/cli1 index.tsx draw --",
    "mcp": "bun run --filter keyboards-mcp serve",
    "jam": "cd apps/mcp && inspector --config inspector.json --server keyboards",
    "jam1": "cd apps/cli1 && inspector --config inspector.json --server keyb"
  },
  "dependencies": {
    "@mcpjam/inspector": "^1.5.12"
  }
}
