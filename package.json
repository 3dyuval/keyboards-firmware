{
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "mcp": "bun run --filter @modelcontextprotocol/server-basic-vanillajs serve",
    "mcp:dev": "bun run --filter @modelcontextprotocol/server-basic-vanillajs dev",
    "mcp:build": "bun run --filter @modelcontextprotocol/server-basic-vanillajs build"
  }
}
